<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="SamTheme" parent="android:Theme.DeviceDefault.Light">
        <!-- Customize your theme here. -->
        <!-- fond app bar  -->
        <!-- fond de la page -->
        <item name="android:windowBackground">@color/sam_fond</item>
        <!-- titre de la page -->
        <item name="android:textColorPrimary">@color/sam_accentuation</item>
    </style>

    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light">
        <!-- Customize your theme here. -->
        <!-- fond app bar  -->
        <!-- fond de la page -->
        <item name="android:windowBackground">@color/sam_fond</item>
        <!-- titre de la page -->
        <item name="android:textColorPrimary">@color/sam_accentuation</item>
    </style>

</resources>
